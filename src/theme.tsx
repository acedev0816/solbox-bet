import { createTheme } from "@mui/material";
const theme = createTheme({
    palette: {
        primary: {
            main: '#dcc057',
        },
        secondary: {
            main: "#a94024"
        },

        
    },
    bg: {
        main: '#aff',
        light: '#fff'
    }
})
export default theme;